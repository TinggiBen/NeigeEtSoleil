{% extends '::layout/layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            {% include '::modulesUsed/navigation.html.twig' %}
            <div class="span9">
                <div class="row">
                    <div class="span5">
                        <img src="{{ asset('img/holder.png') }}" alt="NeigeEtSoleil" width="470" height="310">
                    </div>
                    <div class="span4">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}                       
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}